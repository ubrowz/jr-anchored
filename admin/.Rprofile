# .Rprofile
# NOTE: renv auto-activation is intentionally disabled.
# This project uses a controlled renv library loaded explicitly
# via RENV_PATHS_ROOT in each R script.
# Do not add renv::activate() or source("renv/activate.R") here.
